#include<stdio.h>
#include<stdlib.h>
#include<math.h>

void reorder(int * []);

int main()
{
	int* tab [3];
	while(1)
	{
		scanf("%d", tab[0]);
		scanf("%d", tab[1]);
//		scanf("%d", tab[2]);
		//if(tab[0] == 0 && tab[1] == 0 && tab[0] == 0) break;
//		printf("%d", *tab[0]);
//		reorder(tab);
		

	}
	exit(0);
}

void reorder(int * tab [])
{
	int hasChanged = 1;
	int compteur;
	while(hasChanged)
	{
		for(compteur = 0; compteur < 3; compteur ++)
		{
			//if(
		}
	}

}
